import { DefaultOptionSelectDirective } from './default-option-select.directive';

describe('DefaultOptionSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultOptionSelectDirective();
    expect(directive).toBeTruthy();
  });
});
